<?xml version="1.0" encoding="UTF-8"?>
<!--
    نفس الاختبارات بس على MySQL بدل SQLite.

    السبب: SQLite أسرع وأسهل للتطوير اليومي، بس مشروعك بيشتغل على MySQL،
    وفي فروقات بينهن (تخزين التاريخ والوقت، الـ enum، حذف الـ index وقت
    وجود foreign key). فالتشغيل على MySQL بيكشف مشاكل ما بتظهر على SQLite.

    قبل أول تشغيل:
        CREATE DATABASE school_management_test;

    التشغيل:
        php artisan test -c phpunit.mysql.xml

    تنبيه: RefreshDatabase بيمسح محتوى هالداتابيز كل تشغيل — لهيك هي
    منفصلة عن school_management وما بتتأثر بياناتك.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- ما منبثّ فعلياً بالاختبارات — Reverb مش شغّال، والبث بينفحص بـ Notification::fake -->
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="DB_CONNECTION" value="mysql" force="true"/>
        <env name="DB_DATABASE" value="school_management_test" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
